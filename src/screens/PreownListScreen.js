import React from "react";
import { View, Text, StyleSheet } from "react-native";

const PreownList = () => {
  return (
    <View>
      <Text>PreownList</Text>
    </View>
  );
};

const style = StyleSheet.create({});

export default PreownList;
